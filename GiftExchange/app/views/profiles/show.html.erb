<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <p id="notice"><%= notice %></p>

      <%if @profile.exchange.Deadline != "PASSED" then%>
        <h3>You Have Not Been Assigned A Giftee</h3>
      <%else%>
        <h3>You Have Been Assigned A Giftee<h3>
      <%end%>
      <p>
        <strong>Name:</strong>
        <%= @profile.name %>
      </p>

      <p>
        <strong>Description:</strong>
        <%= @profile.description %>
      </p>

      <p>
        <strong>Address:</strong>
        <%= @profile.address %>
      </p>

      <%if current_user && @profile.user_id == current_user.id then%>
      <p>
        <strong>Your Giftee Is:</strong>
        <%= @profile.giftee%>
        </p>
      <%end%>


      <%= link_to 'Back', :back, {style: 'color:#FFFFFF', class: 'btn btn-large btn-primary'} %>
    </div>
  </div>
</div>
