<%= form_for(@bridge) do |f| %>
  <% if @bridge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bridge.errors.count, "error") %> prohibited this bridge from being saved:</h2>

      <ul>
      <% @bridge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :exchange_id %><br>
    <%= f.number_field :exchange_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
